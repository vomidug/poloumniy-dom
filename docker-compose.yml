version: '3'
services:

  api:
    build: api/
    restart: unless-stopped
    networks:
    - housebot
    - main
    depends_on: 
    - bot
    - mongo

  bot:
    build: bot/
    restart: unless-stopped
    networks:
    - housebot
    - main

  mongo:
    image: 'mongo'
    restart: always
    volumes:
    - mongo:/var/lib/mongodb
    networks:
    - housebot

volumes:
  mongo:

networks:
  housebot:
    external: true
  main:
    external: true
